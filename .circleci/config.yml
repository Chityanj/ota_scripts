version: 2.1
jobs:
  push:
   docker:
      - image: ubuntu:18.04
   steps:
      - run:
          name: init
          command: |
           apt-get -y update && apt-get -y upgrade && apt install python3-pip -y  
           pip3 install python-telegram
           git clone https://github.com/Giovix92/ota_scripts
           bash ota_scripts/push.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - push
